apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-pvc-metrics
  labels:
    monitoring: enabled
    owner: tsisar
    app.kubernetes.io/instance: {{ .Release.Namespace }}
    app.kubernetes.io/name: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - name: pvc-metrics
      port: 2112
      targetPort: 2112